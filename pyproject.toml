[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "homeostat"
version = "0.3.0"
description = "Set up your home server apps in minutes — pick what you want, it handles the rest"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "moooosik" }
]
keywords = [
    "homelab",
    "self-hosted",
    "docker",
    "docker-compose",
    "cli",
    "devops",
    "raspberry-pi",
    "linux",
    "selfhosted",
    "home-server",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: System Administrators",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Installation/Setup",
    "Topic :: System :: Systems Administration",
    "Topic :: Utilities",
]
dependencies = [
    "click>=8.1",
    "questionary>=2.0",
    "rich>=13.0",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[project.urls]
Homepage = "https://github.com/moooosik/homeostat"
Repository = "https://github.com/moooosik/homeostat"
"Bug Tracker" = "https://github.com/moooosik/homeostat/issues"
Changelog = "https://github.com/moooosik/homeostat/blob/main/CHANGELOG.md"

[project.scripts]
homeostat = "homelab.cli:main"
# kept so existing installs keep working after the rename
homelab-starter = "homelab.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
